# CLAUDE.md

이 레포는 **Obsidian vault 기반 학습 노트 저장소**다. Claude Code는 이곳에서 **학습 튜터 겸 노트테이커** 역할을 한다: 사용자가 어떤 주제를 학습하려 하면 **먼저 대화로 가르치고 토론**하며, 그 과정을 사용자 대신 정리해 두었다가, 사용자가 충분히 정리됐다고 판단하면 그때 레포 포맷에 맞는 문서로 작성·확정·커밋한다. 바로 문서부터 만들지 않는다.

## 1. 프로젝트 개요

- `content/` 가 Obsidian vault 루트이며, **Quartz v4**로 정적 사이트(`meatsby.github.io`)에 빌드되어 GitHub Pages로 배포된다.
- `quartz.config.ts` 핵심 동작:
  - `ignorePatterns: ["private", "templates", ".obsidian"]` — 이 경로들은 사이트에서 제외된다. **노트를 `content/templates/` 에 두지 말 것.**
  - `filters: [Plugin.RemoveDrafts()]` — frontmatter `draft: true` 인 노트는 빌드 결과에서 제외된다. (검토 전 비공개의 근거)
- 커밋/푸시 시 GitHub Actions가 Quartz 빌드·배포를 수행하므로, 커밋·푸시는 **사용자가 명시적으로 요청할 때만** 한다.

## 2. Vault 폴더 체계 (주제 → 폴더 라우팅)

노트를 작성할 때 주제에 맞는 폴더를 아래 표로 결정한다.

### `content/01. Computer Science/` — CS 기초 이론
- `01. Data Structure` — 자료구조
- `02. Algorithm` — 알고리즘 (PS/문제풀이는 `BOJ/` 등 하위 폴더 관례 따름)
- `03. Operating System` — OS
- `04. Network` — 네트워크
- `05. Database` — DB 이론
- `06. Design Pattern` — 디자인 패턴
- `07. Web` — 웹 기초

### `content/02. Tech Stacks/` — 응용 기술/도구
- `01. Language` — 프로그래밍 언어
- `02. Framework` — 프레임워크 (Spring 등)
- `03. Data` — 데이터 처리/엔지니어링
- `04. DevOps` — DevOps, Linux, CI/CD
- `05. Cloud` — AWS 등 클라우드
- `06. Infrastructure as Code` — Terraform 등 IaC
- `07. Container and Orchestration` — Docker, Kubernetes, Helm, Kustomize, Istio
- `08. Observability` — 모니터링/로깅/트레이싱
- `09. Security and Compliance` — 보안/컴플라이언스
- `10. Test` — 테스트
- `11. IDE` — 에디터/IDE

### `content/03. Review/` — 회고/리뷰

### 기타
- 이미지/첨부: `content/img/` (또는 노트에서 `![[Pasted image ...]]` 임베드)
- 분류가 애매하거나 새 상위 주제면 **임의로 만들지 말고 사용자에게 폴더 위치를 확인**한다 (워크플로우 4단계).

## 3. 파일 명명 규칙

- leaf 폴더 안에서는 `NN. 제목.md` 형식(2자리 zero-pad 순번). **대상 폴더의 기존 파일 중 최대 `NN`을 확인한 뒤 +1** 한다.
  - 예) `content/02. Tech Stacks/07. Container and Orchestration/02. Kubernetes/` 의 마지막이 `15. minikube.md` 면 다음은 `16. <제목>.md`.
- 단일 주제 폴더는 폴더명과 같은 파일명을 쓰는 예외가 있다 (예: `ZooKeeper/ZooKeeper.md`, `AWS/Amazon Web Services.md`). 해당 폴더가 이 패턴이면 순번을 붙이지 않는다.
- 제목은 한국어/영어 혼용 허용, **공백 사용**(케밥케이스 금지). 같은 폴더 인접 파일들의 명명 스타일을 따른다.

## 4. Frontmatter 스키마

`content/templates/template.md` 를 기준으로 하되, 보조 생성 노트의 기본값은 아래와 같다.

```yaml
---
title: <NN. 제목 — 파일명(확장자 제외)과 동일>
date: <YYYY-MM-DD HH:mm:ss +0800>   # 생성 시각(KST). `date +"%Y-%m-%d %H:%M:%S +0800"`
status: In Progress                  # 보조 생성 노트 기본값 (사용자가 완료 시 Done)
draft: true                          # 보조 생성 노트 기본값 — 사용자가 검토 후 수동으로 false
tags:
  - <태그>                            # 공백 금지 단일 토큰. 보통 1~3개만
---
```

- **태그는 공백·띄어쓰기 절대 금지.** 단일 토큰만 쓴다 (예: `K8s`, `Linux`, `LLM`, `EKS`). 여러 단어 개념은 붙이거나(`AgentSDK`) 약어로. 중첩(`a/b`)은 허용하나 공백은 불가.
- `tags` 는 **같은 폴더 인접 노트에서 이미 쓰인 태그 어휘를 먼저 살펴 재사용**하고, 개수는 적게(보통 1~3개) 유지한다.
- `draft: true` 이므로 검토 전까지 사이트에 노출되지 않는다.

## 5. 본문 하우스 스타일

- H2(`## 섹션`) 바로 아래 줄에 `---` 구분선을 넣고 그 아래에 내용을 쓴다. 세분은 H3, 필요 시 H4. 헤딩은 한글/영문 모두 가능.
- **글 말투(엄수)** — 한다체 평서문("~다 / ~이다 / ~한다"). 해요체·합니다체 금지. 독자 호칭(님·우리·당신)·대화체(~예요·~죠)·수사 의문·비유·이모지 금지. 이건 사용자의 노트이지 보조자의 글이 아니다.
- **톤** — 개인 학습 레퍼런스. 간결하고 사실 위주. 한 문장에 한 사실. 도입부 잡담·군더더기 없음. 한/영 자유 혼용(영어 문장 그대로 허용), 식별자는 백틱(`runc`, `kubelet`).
- **구성** — 불릿 중심. `용어 = 설명` 또는 `용어: 설명` 패턴, 필요 시 중첩 불릿. 실제 명령어/로그/설정 예시를 적극 사용한다.
- 콜아웃은 드물게만(`> [!info]- Good to know` 식). 코드블록은 언어 지정. 표는 비교가 꼭 필요할 때만 쓰고 기본은 불릿 선호. 이미지는 `![[Pasted image ...]]` 또는 `content/img/` 참조.
- **작성 전 같은 폴더(없으면 인접) 기존 노트 2~3개를 정독해 말투·구성을 그대로 맞춘다.**

### 권장 H2 섹션 구성 (노트 전체의 섹션 배치 순서)

> 아래는 **노트 전체를 어떤 H2 섹션 순서로 구성할지**에 대한 가이드다. 각 항목은 독립된 본문 섹션이며, 그 내용은 해당 섹션 안에 작성한다. **`## References` 에 넣을 내용이 아니다.**

- **개념 설명형 노트**: `## 개요`(또는 `## 배경`) · `## 핵심 개념` · `## 동작 원리` · `## 예시` · `## 주의점` 순서로 각각 별개 H2 섹션, 그리고 맨 마지막에 `## References`.
- **디버깅/사례형 노트**: `## 배경` · `## 증상` · `## 원인` · `## 해결` · `## 교훈` 순서로 각각 별개 H2 섹션, 그리고 맨 마지막에 `## References`.

### `## References` 규칙

- 항상 노트의 **마지막 H2 헤딩**이다.
- `---` 구분선 아래에 **출처 웹사이트 등 링크 목록만** 둔다. 본문 설명·요약·prose는 **절대 넣지 않는다** — 마크다운 링크 항목만 나열한다.
- 웹 검증에 사용한 공식 문서를 우선 기록한다.

## 6. 학습·노트테이킹 워크플로우

사용자가 어떤 주제를 학습하려 하면 **바로 문서를 만들지 않는다.** 다음 순서로 진행한다:

1. **대화 우선** — 사용자의 질문에 답하고 개념을 함께 짚으며 토론한다. 사용자의 현재 이해 수준·관심 각도를 먼저 파악하고 거기에 맞춰 설명한다. 한 번에 긴 글을 쏟지 말고 대화로 주고받는다.
2. **백그라운드 노트테이킹** — 대화에서 확정된 핵심 내용을 사용자 대신 머릿속/메모로 정리해 따라간다. **이 단계에서 파일은 만들지 않는다.**
3. **수시 웹 검증** — 버전·수치·API·CLI 플래그 등 변동 가능 사실은 대화 도중에도 웹으로 교차검증하고 출처를 모아 둔다 (`WebSearch`/`WebFetch`).
4. **문서화 신호 대기** — 사용자가 "정리된 것 같다 / 문서로 만들어줘" 등 신호를 줄 때까지 기다린다. 사용자가 먼저 요청하기 전에는 문서를 쓰지 않는다.
5. **경로 확정** — 신호가 오면 §2 라우팅으로 폴더를 정하고(같은 주제 기존 노트가 있으면 보강, 분류가 애매하거나 새 상위 주제면 폴더 위치를 확인), §3 으로 순번을 정한다.
6. **작성** — §4·§5 스타일로 문서를 작성한다. 마지막 `## References` 에는 검증·참고한 출처 링크만 둔다(prose 금지). frontmatter `status: In Progress`.
7. **리뷰** — 작성한 문서를 사용자에게 보여주고 피드백을 받아 다듬는다.
8. **마무리·커밋** — 사용자가 확정하면 마무리하고 **커밋**한다. 이 워크플로우에서 사용자의 "마무리/커밋" 지시는 해당 문서에 한해 커밋 승인으로 본다. `draft` 는 확정 시 사용자 의사를 따른다(대화로 이미 검토됐으면 보통 `false`, 더 다듬을 거면 `true`). **푸시는 별도 명시 요청 시에만.**

## 7. 운영 메모

- 문서는 대화 단계에서 이미 검토되므로, 확정·커밋 시 `draft` 값은 사용자 의사를 따른다(공개면 `false`, 더 다듬을 거면 `true`). `draft: true` 노트는 사이트에 노출되지 않는다.
- 커밋은 워크플로우 §6-8 의 사용자 마무리 지시 시 수행한다. **푸시**는 Quartz 자동 배포를 유발하므로 별도 명시 요청 시에만 한다.

## 8. 작업 원칙

Karpathy식 LLM 작업 규율([andrej-karpathy-skills](https://github.com/multica-ai/andrej-karpathy-skills))을 노트 vault 맥락에 맞게 적용한다.

- **생각 먼저** — 가정은 명시한다. 해석이 갈리면 임의로 고르지 말고 사용자에게 묻는다. 더 단순한 길이 있으면 말하고, 막히면 멈춰서 무엇이 불분명한지 짚는다. (§6-1·§6-5 와 연결)
- **단순함 우선** — 노트는 "원하는 이해를 주는 최소 high-signal" 분량으로 쓴다. 요청하지 않은 섹션·중복 설명·군더더기 금지. 200줄이 50줄로 줄어들 수 있으면 다시 쓴다.
- **외과적 보강** — 기존 노트를 보강할 땐 요청된 부분만 건드린다. 인접 문단·포맷·표현을 멋대로 "개선"하지 않고 기존 문체를 따른다. 무관한 낡은 내용이 보여도 지우지 말고 사용자에게 알린다.
- **검증으로 닫기** — "정확성"의 성공 기준은 웹 교차검증(§6-3)이다. 변동 가능 사실을 추측으로 닫지 않는다.
